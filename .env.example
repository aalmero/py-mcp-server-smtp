# SMTP MCP Server Environment Configuration
# Copy this file to .env and fill in your actual values

# Required SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Optional SMTP Configuration
SMTP_USE_TLS=true
SMTP_USE_SSL=false
SMTP_TIMEOUT=30
SMTP_MAX_RETRIES=3
SMTP_FROM_EMAIL=your-email@gmail.com

# Logging Configuration
LOG_LEVEL=INFO

# HTTP Transport Configuration (when using HTTP mode)
HTTP_PORT=8000

# Multi-Server Configuration Example (optional)
# SMTP_HOST_1=smtp.gmail.com
# SMTP_PORT_1=587
# SMTP_USERNAME_1=user1@gmail.com
# SMTP_PASSWORD_1=password1
# SMTP_USE_TLS_1=true

# SMTP_HOST_2=smtp.outlook.com
# SMTP_PORT_2=587
# SMTP_USERNAME_2=user2@outlook.com
# SMTP_PASSWORD_2=password2
# SMTP_USE_TLS_2=true